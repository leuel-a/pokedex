# Pokedex CLI

A command-line Pokedex built in Go using PokéAPI. Look up Pokémon information such as name, type, and stats directly in your terminal. The REPL supports commands like `help`, `exit`, and `pokemon <name>`.
